name = "tawala"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"

# Note: Cron triggers (0 2 * * * for daily briefing at 05:00 EAT) are configured
# via the Cloudflare Pages dashboard under Settings > Functions > Cron Triggers,
# or via a separate Cloudflare Worker. The [triggers] block is not supported for Pages.
